{
  "name": "figma-designer-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "pnpm -r run build",
    "dev": "pnpm -r run dev",
    "lint": "pnpm -r run lint"
  },
  "devDependencies": {
    "@figma/plugin-typings": "*",
    "@ithinku/tsconfig": "0.0.7",
    "rimraf": "^5.0.0",
    "typescript": "^5.3.2"
  }
}